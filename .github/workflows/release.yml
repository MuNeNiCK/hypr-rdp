name: Release

on:
  push:
    tags: ['v*']

permissions:
  contents: write

env:
  CARGO_TERM_COLOR: always

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libpipewire-0.3-dev libva-dev libxkbcommon-dev libwayland-dev libgbm-dev libpam0g-dev

      - uses: dtolnay/rust-toolchain@stable

      - uses: Swatinem/rust-cache@v2

      - name: Test
        run: cargo test --release

      - name: Build release binary
        run: cargo build --release

      - name: Verify bundled software codec
        run: |
          readelf -d target/release/hypr-rdp > codec-linkage.txt
          if grep -E 'NEEDED.*lib(avcodec|avformat|avutil|swscale|swresample|openh264)' codec-linkage.txt; then
            echo "Unexpected external codec dependency"
            exit 1
          fi

      - name: Package
        run: |
          mkdir -p dist/pkg/pam
          cp target/release/hypr-rdp dist/
          cp LICENSE README.md CHANGELOG.md dist/
          cp pkg/pam/hypr-rdp.* dist/pkg/pam/
          cd dist && tar czf ../hypr-rdp-${GITHUB_REF_NAME}-x86_64-linux.tar.gz *

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
          files: hypr-rdp-*.tar.gz
